library(broom)

broom:glance(model)

broom:tidy(model)

broom:augment(model)

# gets nested r oututs (models and so) into nice tibbles

library(tidyverse)




function(type = c("ddct", "extrapolation")) {
  match.arg(type) # returns argument from type | if none is specified returns the first
}
  

fun_name <- source("path/function_file.R")

# make a plotting function


install.packages("pkgbuild", dependencies = TRUE)


methods(class = "GRanges")